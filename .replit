modules = ["python-3.11", "nodejs-20", "postgresql-16"]
[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start NELLOGISTICS Server"

[[workflows.workflow]]
name = "Start NELLOGISTICS Server"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "python manage.py runserver 0.0.0.0:5000"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 35813
externalPort = 3001

[[ports]]
localPort = 43063
externalPort = 3002

[[ports]]
localPort = 46665
externalPort = 3000

[nix]
channel = "stable-25_05"
packages = ["freetype", "glibcLocales"]

[deployment]
deploymentTarget = "autoscale"
run = ["gunicorn", "--bind=0.0.0.0:5000", "--reuse-port", "--workers=2", "--timeout=120", "--access-logfile", "-", "--error-logfile", "-", "hsamp.wsgi:application"]
build = ["bash", "-c", "echo 'Building NELLOGISTICS...' && cd frontend && npm install && npm run build && cd .. && pip install -r requirements.txt && python manage.py collectstatic --noinput"]
